SSTACK SEGMENT STACK
	   DW 32 DUP(?)		
SSTACK ENDS

CODE SEGMENT
     ASSUME CS:CODE, SS:SSTACK
START:PUSH DS
	  XOR AX,AX		
	  MOV DS,AX
	  MOV SI,3700H		·
	  MOV CX,32			
AA1:  MOV [SI],AL		
	  INC SI			
	  INC AL			
	  LOOP AA1			
	  MOV SI,3700H	
	  MOV DI,3800H	
	  MOV CX,32
AA2:  MOV AL,[SI]		
	  MOV [DI],AL	
	  INC SI
	  INC DI
	  LOOP AA2
	  MOV AX,4C00H	
	  INT 21H		  
	  
CODE  ENDS
	  END START
	  
	  
